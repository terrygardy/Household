@model List<Household.Models.DisplayTable.CDisplayRow>

@foreach (var drRow in Model)
{
	<tr>
		@foreach (var dcColumn in drRow.Columns)
		{
			@Html.Partial("~/Views/Shared/_MasterDataHeaderFooterCellPartial.cshtml", dcColumn)
		}
	</tr>
}